name = "parkko-paymongo"
main = "src/index.js"
compatibility_date = "2024-11-01"

# The PayMongo secret key is NOT stored here. Set it as an encrypted secret:
#   cd worker
#   npx wrangler secret put PAYMONGO_SECRET_KEY
# and paste your sk_test_... key at the prompt. It never touches this repo.
